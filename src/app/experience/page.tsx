import type { Metadata } from "next";
import { Experience } from "@/components/sections/experience";

export const metadata: Metadata = { title: "Experience — M. Awais Nadeem" };

export default function ExperiencePage() {
  return <Experience />;
}
